import { OrganisationsBoard } from "../components/needs/OrganisationsBoard.jsx";

/**
 * Organisations page ("/organisations") — the dedicated, full-page view of
 * the Organisations board. The same board also sits under the hero on the
 * home page ("/") — see DECISIONS.md D-050.
 */
export function DiscoverNeeds() {
  return <OrganisationsBoard />;
}
